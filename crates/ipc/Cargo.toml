[package]
name = "forgeclaw-ipc"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Structured IPC protocol over Unix domain sockets"

[lints]
workspace = true

[dependencies]
# Added as crate develops

[dev-dependencies]
